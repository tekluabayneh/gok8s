package etcd

// TODO
// implement proccess
func proccess(item []Delta, id string) error {
	return nil
}

func delete(item map[string][]Delta, id string) {
	for Itid := range item {
		if Itid == id {
			// remove the entire key value form the times
		}
	}
}
